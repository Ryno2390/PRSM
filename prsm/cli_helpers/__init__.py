"""CLI helper modules.

Sprint 385 — separated from prsm/cli.py so the probe logic
can be unit-tested without going through Click's
CliRunner. Mirrors the pattern used elsewhere in the
codebase (prsm/economy/web3/*).
"""
